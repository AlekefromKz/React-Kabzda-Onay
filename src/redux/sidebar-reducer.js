let initialState = {
    closeFriends: [
        {id: 1, name: 'Aijas'},
        {id: 2, name: 'Oljas'},
        {id: 3, name: 'Mahir'},
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;